import doTests from '../../do.test';

const inputs = [[5, [1, 2, 5]]];
const results = [4];

const dfs = (n, money) => {};

const solution = (n, money) => {
  let answer = 0;
  let counts = dfs(n, money);

  return;
};

doTests(inputs, results, solution);
